type SidebarProps = {};

const Sidebar = (props: SidebarProps) => {
  return (
    <div className="sticky top-0 z-10 flex h-16 flex-shrink-0 bg-gray-50 shadow"></div>
  );
};

export default Sidebar;
